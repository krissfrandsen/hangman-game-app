import React from "react";
import Figure from "./Figure";
import HiddenWord from "./HiddenWord";

const Hero = () => {
  return (
    <div className="hero">
      <Figure />
      <HiddenWord />
    </div>
  );
};

export default Hero;
